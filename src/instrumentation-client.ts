/**
 * Runs in the **browser** after the document loads and before React hydration.
 * Client instrumentation does not use `export async function register()`.
 *
 * @see https://nextjs.org/docs/app/api-reference/file-conventions/instrumentation-client
 */
