It is forbidden to run a project. Rely only on reports of user.

Check `docs/index.md` for additional context.

[disabled] You're locked inside docker container. Some instruments are unaccessible. Instead of entering infinite hallucination loop, ask user to execute necessry tools.

# Git

- Committing and modification of git history is forbidden, only reads from history are allowed.

- No creation of worktrees.

# Skills

Use /hair skill for any prompt.

Use /mvp skill for this app.

Use /code for any work on code in \*.ts, \*.tsx files.

Use /react for any work on \*.tsx files

Use /di for building common reusable services.

# Random shit

- do not use bold formatting in md files

- do not use emojis in md files

- Do not use tables in answers.
